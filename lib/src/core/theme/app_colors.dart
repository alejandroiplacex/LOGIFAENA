import 'package:flutter/material.dart';

class AppColors {
  // Identidad principal de LogiFaena
  static const primary = Color(0xFF0B3A6E);
  static const primaryDark = Color(0xFF062A50);
  static const primaryLight = Color(0xFF2D68A3);

  // Color operativo y de énfasis
  static const accent = Color(0xFFF57C00);
  static const accentLight = Color(0xFFFFB74D);

  // Fondos y superficies
  static const background = Color(0xFFF4F7FB);
  static const surface = Colors.white;
  static const surfaceSoft = Color(0xFFF8FAFD);
  static const border = Color(0xFFD7DEE8);

  // Texto
  static const textPrimary = Color(0xFF17212B);
  static const textSecondary = Color(0xFF5E6B78);
  static const textMuted = Color(0xFF87929E);

  // Estados operacionales
  static const success = Color(0xFF2E7D32);
  static const successSoft = Color(0xFFE8F5E9);

  static const warning = Color(0xFFF9A825);
  static const warningSoft = Color(0xFFFFF8E1);

  static const danger = Color(0xFFC62828);
  static const dangerSoft = Color(0xFFFFEBEE);

  static const info = Color(0xFF1976D2);
  static const infoSoft = Color(0xFFE3F2FD);

  // Estados logísticos
  static const pending = Color(0xFF757575);
  static const traveling = Color(0xFF1565C0);
  static const lodging = Color(0xFF6A1B9A);
  static const transfer = Color(0xFFEF6C00);
  static const atSite = Color(0xFF2E7D32);
  static const finished = Color(0xFF455A64);
  static const cancelled = Color(0xFFC62828);

  const AppColors._();
}
